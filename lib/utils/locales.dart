import 'dart:ui';

class Locales {
  static var list = <Locale>[
    const Locale('en', ''),
    const Locale('id', ''),
    const Locale('ar', ''),
  ];

  static List<Locale> get locales => list;
}
